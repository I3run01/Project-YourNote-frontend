import styled from "styled-components";

type props = {
    isDark: boolean
}

export const LayoutStyled = styled.div`
    height: 100vh;
    display: flex;
    flex-direction: flex-start;

    overflow-y: hidden;
`